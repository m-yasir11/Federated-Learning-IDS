# Federated-Learning-IDS
A distributed Intrusion Detection System (IDS) built using Federated Learning (FL) and Deep Learning (MLP) to detect network attacks while preserving data privacy. This project leverages the Flower (flwr) framework and the UNSW-NB15 dataset to simulate a multi-client environment where models are trained collaboratively without sharing raw data.
